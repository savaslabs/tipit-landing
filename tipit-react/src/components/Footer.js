import React from 'react';
import { Dashes } from './Dashes';

export const Footer = () => (
    <div style={{position: 'fixed', left: 0, right: 0, bottom: 10, maxWidth: '100%'}}>
        <Dashes />
    </div>    
)
